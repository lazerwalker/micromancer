* Style keyboard
* Style selected tokens
* There's a faux extra token at the end of the list that's selected / you can click on
* Enter labels
* Show labels on the view
* Can tap a label to insert it if in an operand
* Syntax highlighting
* Negative numbers
* Math won't let you enter multiple symbols after another
* Parens?
* Animate keyboard
* Long-press an opcode to get a description
* Style everything to look old-timey
* Indicate invalid lines, and empty token spaces
* Empty token spots have a min-width
* When highlighting an indirect operand, subtly highlight the thing it refers to
* EQU label entry
* Custom keyboard for labels / EQU
* Spaces in math